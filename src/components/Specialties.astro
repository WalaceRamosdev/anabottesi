---
const specialties = [
    {
        title: "Dependência Emocional",
        description:
            "Liberte-se da necessidade constante de validação externa e construa uma relação saudável consigo mesma.",
        icon: "🤝",
        delay: 0,
    },
    {
        title: "Exaustão Mental",
        description:
            "Aprenda a lidar com o burnout emocional e a exaustão que vem de tentar dar conta de tudo sozinha.",
        icon: "🧘‍♀️",
        delay: 0.2,
    },
    {
        title: "Relacionamentos Abusivos",
        description:
            "Identifique padrões prejudiciais, reconstrua sua autoestima e encontre o caminho de volta para sua liberdade.",
        icon: "🔒",
        delay: 0.4,
    },
];
---

<section id="especialidades" class="bg-brand-bg py-24 relative overflow-hidden">
    <div class="max-w-7xl mx-auto px-4 md:px-8">
        <div class="text-center mb-16">
            <h2
                class="text-brand-primary text-sm font-bold uppercase tracking-widest mb-4"
            >
                Como posso ajudar
            </h2>
            <h3 class="text-4xl md:text-5xl font-bold">Especialidades</h3>
        </div>

        <div class="grid md:grid-cols-3 gap-8">
            {
                specialties.map((item) => (
                    <div class="spec-card group bg-white p-10 rounded-3xl shadow-sm border border-brand-primary/10 hover:shadow-xl transition-all duration-500 hover:-translate-y-2">
                        <div class="w-16 h-16 bg-brand-secondary/20 rounded-2xl flex items-center justify-center text-3xl mb-8 group-hover:bg-brand-primary group-hover:text-white transition-colors duration-500">
                            {item.icon}
                        </div>
                        <h4 class="text-24 font-bold mb-4">{item.title}</h4>
                        <p class="text-brand-text/70 leading-relaxed mb-8">
                            {item.description}
                        </p>
                        <a
                            href="https://linktr.ee/apbottesi"
                            target="_blank"
                            class="inline-flex items-center gap-2 text-brand-primary font-bold text-sm tracking-wide uppercase hover:gap-3 transition-all"
                        >
                            Entender como tratar <span>→</span>
                        </a>
                    </div>
                ))
            }
        </div>
    </div>
</section>

<script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";

    gsap.registerPlugin(ScrollTrigger);

    gsap.from(".spec-card", {
        scrollTrigger: {
            trigger: "#especialidades",
            start: "top 80%",
        },
        y: 60,
        opacity: 0,
        stagger: 0.2,
        duration: 1,
        ease: "power3.out",
    });
</script>
